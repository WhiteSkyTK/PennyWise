<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    android:layout_marginHorizontal="8dp"
    app:cardElevation="4dp"
    app:cardCornerRadius="12dp"
    app:cardBackgroundColor="?attr/colorOnBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Category Type (e.g., Fixed or Variable) -->
        <TextView
            android:id="@+id/categoryType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Fixed"
            android:textSize="12sp"
            android:textColor="@color/main_purple"
            android:fontFamily="@font/nunito_medium"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:layout_marginBottom="8dp" />

        <!-- Main Row: Category Name and Options -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:id="@+id/categoryName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Category Name"
                android:textSize="16sp"
                android:textColor="?attr/colorBackground"
                android:fontFamily="@font/nunito_bold" />

            <ImageView
                android:id="@+id/optionsIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_more_vert"
                android:contentDescription="options"
                app:tint="?attr/colorBackground" />
        </LinearLayout>

        <!-- New TextView: Amount Used -->
        <TextView
            android:id="@+id/amountUsedText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="R 0.00 used"
            android:textSize="14sp"
            android:textColor="@color/gray_dark"
            android:fontFamily="@font/nunito_medium" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
